// check two numbers if one of them is 100  or the sum of it => return true , else =? return false

const is100 = (x, v) => {
  return x === 100 || y === 100 || x + y === 100 ? true : false;
};

console.log();
